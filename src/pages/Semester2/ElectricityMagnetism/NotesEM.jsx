import DownloadCard from "../../../components/DownloadCard";

const NotesEM = ({dark}) => {
  return (
    <DownloadCard
      chapterTitle="Electricity and Magnetism Notes"
      pdfLink="#"
      dark={dark}
    />
  );
};

export default NotesEM;
