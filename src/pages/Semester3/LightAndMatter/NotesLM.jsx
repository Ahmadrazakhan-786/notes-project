import DownloadCard from "../../../components/DownloadCard";

const NotesLM = ({dark}) => {
  return (
    <DownloadCard
      chapterTitle="Light and Matter Notes"
      pdfLink="#"
      dark={dark}
    />
  );
};

export default NotesLM;
