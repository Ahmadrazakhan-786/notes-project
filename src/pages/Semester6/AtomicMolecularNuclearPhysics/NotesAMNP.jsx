import DownloadCard from "../../../components/DownloadCard";

const NotesAMNP = ({dark}) => {
  return (
    <DownloadCard
      chapterTitle="Atomic Molecular Notes"
      pdfLink="#"
      dark={dark}
    />
  );
};

export default NotesAMNP;
